interface:
  display_name: "Trails"
  short_description: "Turn waypoints or a walk guide into a clean GPX on real paths"
  default_prompt: "Use $trails to build a followable GPX route from these waypoints."
